spring.application.name=spring-boot-ecommerce
server.port=9898

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#mysql local db
spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=ecommerceapp
spring.datasource.password=ecommerceapp
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


spring.data.rest.base-path=/api
allowed.origins=https://localhost:4200

#it will only expose those REST API's that is annotated with @RepositoryRestResource annotation
spring.data.rest.detection-strategy=ANNOTATED

okta.oauth2.client-id=0oahvv4pv8QBzUTLQ5d7
okta.oauth2.issuer=https://dev-78565656.okta.com/oauth2/default

#####
#
# HTTPS configuration
#
#####

# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true

# Alias that identifies the key in the key store
server.ssl.key-alias=luv2code

# Keystore location
server.ssl.key-store=classpath:ecommerce-keystore.p12

# Keystore password
server.ssl.key-store-password=secret

# Keystore format
server.ssl.key-store-type=PKCS12

#RazorPay client id and secret
razorpay.key.id=rzp_test_21ME8BDB1Ey67B
razorpay.key.secret=dmba9pGxROTjqcssS1cUjCdW

spring.profiles.active=${ENV:dev}
